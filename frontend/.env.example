# Server-only: internal address of the Tonemill API (e.g. http://api:8000 inside Docker).
# Never exposed to the browser -- see src/routes/api/[...path]/+server.ts.
TONEMILL_API_BASE_URL=http://localhost:8000
